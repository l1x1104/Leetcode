#1 Recursive Solution
Nice use of target, but also can remove the instance variable.
class Solution {
    public int longestConsecutive(TreeNode root) {
        if(root == null) return 0;
        int[] count = new int[1];
        dfs(root, count, root.val, 0);
        return count[0];
    }
    public void dfs(TreeNode root, int[] count, int target, int sum) {
        if(root == null) return ;
        if(root.val == target) sum ++;
        else sum = 1;
        count[0] = Math.max(count[0], sum);
        dfs(root.left, count, root.val + 1, sum);
        dfs(root.right, count, root.val + 1, sum);
    }
}

#2
class Solution {
    public int longestConsecutive(TreeNode root) {
        if(root == null) return 0;
        return helper(root, root.val + 1, 1, 1);
    }
    public int helper(TreeNode root, int target, int sum, int max) {
        if(root == null) return max;
        if(root.val == target) {
            sum ++;
            max = Math.max(max, sum);
        }else {
            sum = 1;
        }
        int left = helper(root.left, root.val + 1, sum, max);
        int right = helper(root.right, root.val + 1, sum, max);
        return Math.max(left, right);
    }
}
